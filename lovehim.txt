<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>For My Love 💖</title>

  <!-- Google Font (optional) -->
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
      background: #fff0f5;
      text-align: center;
      position: relative;
    }

    /* Heart shapes floating */
    .heart {
      position: absolute;
      width: 25px;
      height: 25px;
      background: #ff69b4;
      transform: rotate(45deg);
      opacity: 0.8;
      animation: floatUp linear infinite;
    }
    .heart::before,
    .heart::after {
      content: "";
      position: absolute;
      width: 25px;
      height: 25px;
      background: #ff69b4;
      border-radius: 50%;
    }
    .heart::before { top: -12.5px; left: 0; }
    .heart::after  { left: -12.5px; top: 0; }

    @keyframes floatUp {
      0% { transform: translateY(0) rotate(45deg); opacity: 1; }
      100% { transform: translateY(-120vh) rotate(45deg); opacity: 0; }
    }

    /* Images section */
    #photos {
      position: relative;
      z-index: 2;
      margin-top: 50px;
    }
    #photos img {
      width: 180px;
      margin: 15px;
      border-radius: 50%;
      border: 4px solid #fff;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }

    /* Main text */
    #mainText {
      font-family: 'Great Vibes', cursive;
      font-size: 48px;
      color: #d63384;
      margin-top: 30px;
      position: relative;
      z-index: 2;
    }

    /* Click button */
    .loveBtn {
      background: #ff69b4;
      color: white;
      border: none;
      padding: 15px 30px;
      font-size: 20px;
      border-radius: 30px;
      cursor: pointer;
      margin-top: 20px;
      z-index: 2;
      position: relative;
    }
    .loveBtn:hover {
      background: #ff85c1;
    }
  </style>
</head>

<body>

  <div id="mainText">Click below to enter our world 💘</div>

  <button class="loveBtn" onclick="showContent()">Enter ❤️</button>

  <div id="photos" style="display:none;">
    <!-- Replace these URLs with your own pictures -->
    <img src="https://photos.app.goo.gl/iNbe1ZPuBUikxu8K7.jpg">
    <img src="https://photos.app.goo.gl/yoNh5pj3g6SAKXDJ9.jpg">
  </div>

  <script>
    // Create floating hearts
    for (let i = 0; i < 30; i++) {
      const h = document.createElement("div");
      h.classList.add("heart");
      h.style.left = Math.random() * 100 + "vw";
      h.style.animationDuration = (4 + Math.random() * 4) + "s";
      h.style.width = h.style.height = (15 + Math.random() * 25) + "px";
      document.body.appendChild(h);
    }

    // Show photos and change text after click
    function showContent() {
      document.getElementById("photos").style.display = "block";
      document.getElementById("mainText").textContent = "For My Love ❤️";
    }
  </script>

</body>
</html>